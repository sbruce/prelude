;; org-mode configuration

(use-package org-mode
  :defer f
  :custom
  (whitespace-line-column 200 "Increase the column width")
  (org-directory "~/Tasks" "Set the default Tasks path for all machines")
  )
